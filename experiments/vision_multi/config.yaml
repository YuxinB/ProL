t: 500
method: "proformer"
device: "cuda:2"
epochs: 500
batchsize: 64
augment: False

defaults:
  - override hydra/launcher: joblib

hydra:
  launcher:
    # override the number of jobs for joblib
    n_jobs: 3